[Partition]
Type=esp
Format=vfat
# 1G: the NVIDIA 595 kernel modules inflate the kernel-modules initrd
# beyond what a 512M ESP can hold (mcopy "Disk full"). Same size as
# tdx-gpu for the same reason.
SizeMinBytes=1G
SizeMaxBytes=1G
CopyFiles=/boot:/
CopyFiles=/efi:/
