[Match]
Profiles=dev

[Content]
# GCE SSH key provisioning - dev builds only (production has no sshd).
# google-compute-engine pulls openssh-server; oslogin provides the NSS
# and AuthorizedKeysCommand machinery used by 60-gce.conf.
Packages=
    google-compute-engine
    google-compute-engine-oslogin
